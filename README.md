A very simple quickly made c# console project, that renders a orthographic spinning cube in the terminal (z-depth gradient using asci-symbols)
this was made in about an hour, so the code quality is not exactly good
